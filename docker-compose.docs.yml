services:
  docs:
    image: geocos_backend_local_docs
    container_name: geocos_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./geocos_backend:/app/geocos_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
